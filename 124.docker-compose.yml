version: '3.8'
services:
  frontend:
    image: nginx:alpine # Placeholder - Se o seu frontend for uma aplicação do monorepo, altere para 'ethos-monorepo' e defina APP_NAME
    ports:
      - "80:80"
    # Se for uma aplicação do monorepo, descomente e ajuste:
    # environment:
    #   - APP_NAME=nome-do-seu-frontend # Ex: se for 'apps/my-frontend'
    # volumes:
    #   - ./frontend_dist:/usr/share/nginx/html # Exemplo para servir arquivos estáticos
